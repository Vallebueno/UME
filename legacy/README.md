# Legacy Material

This directory contains historical, superseded, exploratory, or non-core files
that were removed from the main public workflow surface during repository
cleanup.

These files are retained for provenance and review, but they are not required
to run the active workflow centered on:

- `code/UMCAL/src/UME_RCALL_V2.1.sh`

Where legacy text files contained local cluster paths or usernames, those
references were replaced with generic placeholders for public release.
